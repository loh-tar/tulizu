To install from source simple run:
  sudo make install

After that it may wise to run:
  tulizu doc man    # Most notably chapter 3

To remove, which you will of cause never do, run:
  sudo make uninstall
